# vsos.conf — declarative VSOS config
# Describe the desired state; the kernel reads this at boot and applies it.

hostname = "void"
version = "1.3"
motd = "Welcome to VSOS 1.3 - Void Shell OS. Type 'help' to see available commands."
