# build artifacts — never commit these, they're regenerated by `make`
*.o
*.bin
os-image.bin
qemu.log

# editor/OS cruft
.DS_Store
*.swp

# filesystem disk: runtime state (your files inside VSOS), not a build artifact
fs.img
